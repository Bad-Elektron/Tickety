/// Core application state management.
///
/// Note: Authentication state is managed via Riverpod in `core/providers/auth_provider.dart`.
/// This module only contains non-auth app state (debug mode, account tier).
library;

export 'app_state.dart';
